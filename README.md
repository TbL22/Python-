# CampusPocket 校园账本

一个 GitHub 仓库保存第 1—9 章的代码，按章保留文件，并用 Git 提交与标签保存修改历史。

## 当前交付：第二版（第 2 章）

以最新提供的 `chapter02_题目(1).txt` 为准，交付文件为 `CampusPocket/chapter02.py`。

在项目根目录运行：

```powershell
python CampusPocket/chapter02.py
```

依次输入一笔消费和月度预算，得到余额、已用比例和三档提醒：低于 80%、80%—100%（含两端）、超过 100%。三个选做项也已完成：金额两位小数、比例一位小数；判断整数元；计算还可支付的同额消费笔数。

本章假定输入合法：消费不小于零、预算大于零、金额最多两位小数。按题目要求，非法金额处理留到第 9 章。零元消费不会计算有限笔数，超预算时可再支付笔数为零。

## 文件与版本

- `CampusPocket/chapter02.py`：本次第二版交付，标签 `chapter02-v0.2.0`。
- `main.py`：保留此前实现，不覆盖；此前标签 `chapter02-v0.1.0` 仍保留。
- 当前没有 `chapter01.py`，未虚构上一章文件。因此无法执行题目所说的从 chapter01.py 复制这一步；收到真实上一章文件后可以补入目录。
- 之前收到的第 3 章要求尚未实施，以这次更正的第 2 章文件为准。

## 运行验收

| 消费 / 预算 | 预期 |
| --- | --- |
| 25 / 100 | 余额 75.00，已用 25.0%，低档，还能 3 笔，是整数元 |
| 80 / 100 | 中间档 |
| 100 / 100 | 中间档，余额 0.00 |
| 120 / 100 | 高档，余额 -20.00，还能 0 笔 |
| 79.99 / 100 | 低档；显示可能舍入为 80.0%，判断仍按真实金额 |
| 100.01 / 100 | 高档 |
| 0.10 / 0.30 | 余额 0.20，还能 2 笔，不是整数元 |
| 0 / 100 | 提示无法计算有限笔数 |

## 课后思考参考

1. 容易出错的是输入类型转换、80% 和 100% 的边界以及小数运算。应先将输入转为数字，明确两个边界都属于中间档。内部用整数分计算，可以避免同额消费笔数受到浮点误差影响。
2. 数据增多后，可在后续章节用列表保存多笔消费，再学习循环统一处理；本章仍只处理一笔。

自动历史任务只处理已纳入 Git 的文件，运行方式见 HISTORY.md。电脑重启后需要重新启动。
